spring:
  jmx:
    enabled: false
  main:
    lazy-initialization: true
  datasource:
    url: jdbc:h2:file:~/usuarios_previred
    driverClassName: org.h2.Driver
    username: sa
    password: completo
  h2:
    console:
      enabled: true
      path: /h2-console
  web:
    resources:
      cache:
        period: 0